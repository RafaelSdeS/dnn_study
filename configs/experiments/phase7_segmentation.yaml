# Phase 7 segmentation experiments — per-backbone configs
# Matches phase7_detection.yaml's epochs/lr/early_stopping_patience.

alexnet_bottleneck:
  data:
    batch_size: 16
  trainer:
    epochs: 1000
    lr: 1.0e-4
    early_stopping_patience: 50

alexnet_fire:
  data:
    batch_size: 16
  trainer:
    epochs: 1000
    lr: 1.0e-4
    early_stopping_patience: 50

alexnet_tv:
  data:
    batch_size: 16
  trainer:
    epochs: 1000
    lr: 1.0e-4
    early_stopping_patience: 50
