# Protocol-matched pair for phase_11_kernel_size_comparison's alexnet_tv_3x3: alexnet_3x3_fc under
# the SAME 500 ep / no early stopping / uniform lr (_protocols/no_patience.yaml), FP32 only. Its
# earlier value (35.79%, Phase 2: early stopping, 37 ep) vs alexnet_tv_3x3 (26.50%) differs in
# geometry AND protocol (worth ~+7pp on its own) AND Dropout AND init; this run removes the
# protocol term. Dropout and init (he_init in alexnet_tv_3x3, default here) still differ.
# Submit: python -m scripts.cluster submit --experiment phase_11_geometry_controls_500ep --runtime pcad --slurm tupi_4090
extends: _protocols/no_patience
name: phase_11_geometry_controls_500ep
stages:
  - fp32
models:
  - alexnet_3x3_fc
