# Phase 7 segmentation training config
# Provisional defaults — VOC 2012 segmentation is ~10x smaller than detection
# (~1.5K/1.5K images vs ~16.5K/4.9K), no real training-budget decision made yet.
# Shares detection.yaml's data/trainer defaults (voc_root, workers, lr, etc.) via
# extends: -- only img_size/epochs/early_stopping_patience diverge for this task.
extends: detection

data:
  img_size: 256

trainer:
  epochs: 50
  early_stopping_patience: 15
